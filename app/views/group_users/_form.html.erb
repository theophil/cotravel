<%= form_for(@group_user) do |f| %>
  <% if @group_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_user.errors.count, "error") %> prohibited this group_user from being saved:</h2>

      <ul>
      <% @group_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.text_field :group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
