<%= simple_form_for @group_user, html: { class: 'form-horizontal' } do |f| %>
  <% if @group_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_user.errors.count, "error") %> prohibited this group_user from being saved:</h2>

      <ul>
      <% @group_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :user_id, collection: User.alphabetical.all, prompt: "Select Group..." %>
  </div>
  <div class="field">
    <%= f.input :group_id, collection: Group.alphabetical.all, prompt: "Select Group..." %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
