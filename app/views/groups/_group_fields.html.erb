<%= simple_form_for @group, html: { class: 'form-horizontal' } do |f| %>
	<%= f.input :name, placeholder: 'Name'%>

	<%= f.input :group_admin_id, collection: User.alphabetical.all, prompt: "Select Group Administrator..." %>

<%end%>